# Releasing a new version of SENTIL

Bump the version across every package, and update the changelog. Then, run the CI and make sure everything passes. Finally, tag with `v` and the version, and push the tag.