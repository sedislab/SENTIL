[package]
name = "sentil-embedded-deployment"
version.workspace = true
edition = "2021"
publish = false
description = "Per-cycle latency harness for the Raspberry Pi safety-monitoring deployment"

[dependencies]
sentil = { path = "../../sentil-core" }
serde = { workspace = true }
serde_json = "1.0"
rand = { workspace = true }
rand_chacha = { workspace = true }

[[bin]]
name = "embedded_deployment"
path = "src/main.rs"