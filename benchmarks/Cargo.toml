[package]
name = "sentil-benchmarks"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sentil = { path = "../sentil-core" }
serde = { workspace = true }
serde_json = "1.0"
rand = { workspace = true }
rand_chacha = { workspace = true }